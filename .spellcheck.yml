matrix:
  - name: Markdown
    sources:
      - '**/*.md'
    exclude:
      - TODO.txt
    dictionary:
      word_lists:
        - en_US
        - ./allowlist.txt
      ignore_case: true
    filters:
      - type: regex
        options:
          pattern: '(```.*?```|`.*?`)'
          replacement: ''
      - type: regex
        options:
          pattern: '\bhttps?://[^\s\)\]]+'
          replacement: ''
