matrix:
  - name: Markdown
    sources:
      - '**/*.md'
    exclude:
      - TODO.txt
    dictionary:
      word_lists:
        - en_US
        - ./allowlist.txt
    filters:
      - type: wordlist
        ignore_case: true
    pipeline:
      remove_code:
        options:
          regex: '(```.*?```|`.*?`)'
          replacement: ''
      remove_urls:
        options:
          regex: '\bhttps?://[^\s\)\]]+'
          replacement: ''
